<h2>My Heroes</h2>

<!-- ここまでヒーローリストの表示 -->
<ul class="heroes">
  <li *ngFor="let hero of myHeroes">
    <a routerLink="/detail/{hero.id}}">
      <span class="badge">{{hero.id}}</span> {{hero.name}}
    </a>
  </li>
</ul>
